"""
Infrastructure Layer - External services and technical implementations.

This package contains implementations of technical concerns:
- Database connections and sessions (PostgreSQL)
- Cache implementations (Redis, Materialized Views, Embedding Cache)
- External service clients (Mem0, RabbitMQ)
- Message queue services
- Vector store integrations

The infrastructure layer implements interfaces defined in the domain layer
and provides technical capabilities to the application.
"""

